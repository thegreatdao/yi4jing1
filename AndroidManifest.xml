<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="iching.android.activities"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".IChing"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    	<activity android:name=".About" android:label="@string/about_label"
    		android:theme="@android:style/Theme.Dialog"/>
    		 
    	<activity android:name=".IChingGridView" android:label="@string/gua_label"/>
    		
    	<activity android:name=".Gua" android:label="@string/gua_label" android:theme="@android:style/Theme.Dialog"/>
	</application>
    
    <uses-sdk android:minSdkVersion="8" />

</manifest> 